# Build Instructions

These are instructions for Mac. I think this also compatibile with Linux, but I'm unsure.
1. Run `brew install sfml`
2. Build with `make build`
3. Run with `./build/mandelbrot <RESOLUTION_X> <RESOLUTION_Y>`